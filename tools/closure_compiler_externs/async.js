var async = {};

/**
 * @param {Array<function(function(*=))>} tasks
 * @param {function(*=)=} cb
 */
async.series = function(tasks, cb) {};

/**
 * @param {Array<function(function(*=))>} tasks
 * @param {function(*=)=} cb
 */
async.parallel = function(tasks, cb) {};
